{
  "name": "sehat-sync-ai-root",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "axios": "^1.6.0",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "firebase-admin": "^11.11.1",
    "socket.io": "^4.7.2"
  },
  "scripts": {
    "vercel-install": "cd server && npm install && cd ../client && npm install",
    "vercel-build": "cd client && npm run build"
  }
}
